#!/usr/bin/env python3
"""
Launch navigation_node node

This file was auto-generated by RoboDSL.
"""

from launch import LaunchDescription
from launch_ros.actions import Node
from launch.actions import DeclareLaunchArgument
from launch.substitutions import LaunchConfiguration

def generate_launch_description():
    # Declare launch arguments
    launch_arguments = [
        DeclareLaunchArgument(
            'use_sim_time',
            default_value='false',
            description='Use simulation time'
        ),
        DeclareLaunchArgument(
            'log_level',
            default_value='info',
            description='Log level for the node'
        ),
    ]
    
    # Create node
    navigation_node_node = Node(
        package='robodsl_package',
        executable='navigation_node_node',
        name='navigation_node_node',
        namespace='/navigation_node',
        output='screen',
        parameters=[
            {'max_velocity': ""},
            {'goal_tolerance': ""},
            {'enable_obstacle_avoidance': False},
            {'use_sim_time': LaunchConfiguration("use_sim_time")},
        ],
        arguments=['--ros-args', '--log-level', LaunchConfiguration('log_level')],
    )

    return LaunchDescription(launch_arguments + [navigation_node_node])
