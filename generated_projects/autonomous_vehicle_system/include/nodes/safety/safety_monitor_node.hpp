// Generated by RoboDSL - DO NOT EDIT

#ifndef SAFETY_MONITOR_NODE_HPP
#define SAFETY_MONITOR_NODE_HPP

// Standard includes
#include <memory>
#include <string>
#include <vector>
#include <map>
#include <cmath>
#include <functional>
#include <thread>
#include <atomic>

// ROS2 includes
#include <rclcpp/rclcpp.hpp>

// Message includes
#include <std_msgs/msg/bool.hpp>
#include <std_msgs/msg/float64_multi_array.hpp>
#include <std_msgs/msg/string.hpp>

// Additional ROS2 includes

// CUDA includes (if needed)

// OpenCV includes (if needed)

// Type definitions
#ifndef __UCHAR_TYPE__
typedef unsigned char uchar;
#endif

// Forward declarations for custom types

// CUDA parameter structs


class Safety_monitorNode : public rclcpp::Node {
public:
    explicit Safety_monitorNode(const rclcpp::NodeOptions& options = rclcpp::NodeOptions());
    virtual ~Safety_monitorNode();


    // Timer callbacks
    void on_safety_timer();

    // Subscriber callbacks
    void on_vehicle_kinematic_state(const std_msgs::msg::Float64MultiArray::ConstSharedPtr msg);
    void on_safety_monitoring(const std_msgs::msg::Bool::ConstSharedPtr msg);

    // Service callbacks



    // User-defined C++ methods

private:
    // Publishers
    std::shared_ptr<rclcpp::Publisher<std_msgs::msg::Bool>> safety_emergency_stop_pub_;
    std::shared_ptr<rclcpp::Publisher<std_msgs::msg::String>> safety_safety_status_pub_;

    // Subscribers
    rclcpp::Subscription<std_msgs::msg::Float64MultiArray>::SharedPtr vehicle_kinematic_state_sub_;
    rclcpp::Subscription<std_msgs::msg::Bool>::SharedPtr safety_monitoring_sub_;

    // Services


    // Timers
    rclcpp::TimerBase::SharedPtr safety_timer_timer_;

    // Lifecycle state tracking

    
    // Processing control
    bool enable_processing_{true};

    // Parameters
    double safety_check_frequency_;
    double emergency_threshold_;

    // Latest message storage (per-subscriber)
    std_msgs::msg::Float64MultiArray::ConstSharedPtr latest_vehicle_kinematic_state_;
    std_msgs::msg::Bool::ConstSharedPtr latest_safety_monitoring_;

    // CUDA kernel member variables

    // Helper methods
    void init_parameters();
    void init_publishers();
    void init_subscribers();
    void init_services();
    void init_timers();
};


#endif // SAFETY_MONITOR_NODE_HPP
